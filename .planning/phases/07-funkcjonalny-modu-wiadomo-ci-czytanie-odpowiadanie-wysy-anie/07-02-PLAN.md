---
phase: 07-funkcjonalny-modu-wiadomo-ci-czytanie-odpowiadanie-wysy-anie
plan: 02
type: execute
wave: 1
depends_on:
  - 07-01
files_modified:
  - lib/data/repositories/school_repository.dart
  - lib/data/repositories/mock_school_repository.dart
  - lib/data/repositories/firestore_school_repository.dart
  - lib/presentation/screens/messages/message_thread_screen.dart
  - lib/presentation/screens/messages/messages_screen.dart
  - lib/presentation/screens/main_navigation_screen.dart
  - lib/presentation/widgets/app_header.dart
autonomous: true
requirements:
  - REQ-MSG-08
must_haves:
  truths:
    - "Otwarcie wątku wiadomości automatycznie oznacza wiadomość jako przeczytaną w repozytorium i aktualizuje stan aplikacji"
    - "W widoku wątku (MessageThreadScreen) użytkownik może ręcznie oznaczyć wiadomość jako nieprzeczytaną/przeczytaną za pomocą przycisku na AppBarze"
    - "Na ikonie wiadomości na dolnym pasku nawigacji oraz w nagłówku wyświetla się badge z dokładną liczbą nieprzeczytanych wiadomości"
    - "Gdy liczba nieprzeczytanych wiadomości wynosi 0, badge z licznikiem jest całkowicie ukrywany"
    - "W zakładce Wiadomości badge nie wyświetla łącznej liczby wszystkich wiadomości, lecz jedynie liczbę nieprzeczytanych"
---

<objective>
Implementacja dynamicznego oznaczania wiadomości jako nowe i przeczytane (REQ-MSG-08) z natychmiastową persystencją oraz wdrożeniem dokładnego licznika nieprzeczytanych wiadomości w postaci badge na ikonie modułu wiadomości (dolny pasek nawigacji i nagłówek).

Purpose: Użytkownik natychmiast widzi czy i ile ma nowych wiadomości oraz może wygodnie zarządzać stanem przeczytania.
Output: Metody oznaczania w repozytoriach, automatyczne oznaczanie w MessageThreadScreen i dynamiczne wskaźniki badge.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Metody oznaczania przeczytania w repozytorium (SchoolRepository & Implementacje)</name>
  <files>
    lib/data/repositories/school_repository.dart
    lib/data/repositories/mock_school_repository.dart
    lib/data/repositories/firestore_school_repository.dart
  </files>
  <action>
    1. W `lib/data/repositories/school_repository.dart`:
       - Dodaj `Future<void> markMessageAsRead(String msgId, {bool isRead = true});`
       - Dodaj `Future<void> markAllMessagesAsRead();`
    2. W `lib/data/repositories/mock_school_repository.dart`:
       - Zaimplementuj `markMessageAsRead`: znajdź wątek po `msgId`, ustaw `isUnread: !isRead`.
       - Zaimplementuj `markAllMessagesAsRead`: ustaw `isUnread = false` dla wszystkich wątków.
    3. W `lib/data/repositories/firestore_school_repository.dart`:
       - Przechowuj lokalny zbiór `_readOverrides: Map<String, bool>` w pamięci i SharedPreferences, aby zmiany stanu były natychmiast widoczne w UI bez czekania na sieć.
       - W `getMessages()` uwzględniaj `_readOverrides` podczas budowania `MessageThread.isUnread`.
       - Jeśli użytkownik jest połączony z kontem Librus w Firestore, zaktualizuj tablicę `messages` w dokumencie `students/{login}` w tle.
  </action>
  <verify>flutter analyze</verify>
  <acceptance_criteria>
    - `markMessageAsRead` i `markAllMessagesAsRead` kompilują się i są poprawnie zaimplementowane
    - Zmiana stanu przeczytania wpływa natychmiast na zwracaną listę `getMessages()`
  </acceptance_criteria>
  <done>Warstwa danych wspiera zarządzanie stanem przeczytania wiadomości</done>
</task>

<task type="auto">
  <name>Task 2: Automatyczne i ręczne oznaczanie w widoku wątku (MessageThreadScreen)</name>
  <files>
    lib/presentation/screens/messages/message_thread_screen.dart
  </files>
  <action>
    1. W `MessageThreadScreenState.initState`:
       - Jeśli `_currentThread.isUnread`, wywołaj `ref.read(schoolRepositoryProvider).markMessageAsRead(_currentThread.id, isRead: true)`.
       - Zaktualizuj stan lokalny `_currentThread = _currentThread.copyWith(isUnread: false)` i zainwaliduj `messagesProvider`.
    2. W obsłudze przycisku koperty w AppBarze:
       - Nowy stan: `final newStatus = !_currentThread.isUnread;`
       - Wywołaj `ref.read(schoolRepositoryProvider).markMessageAsRead(_currentThread.id, isRead: !newStatus)`.
       - Zaktualizuj `_currentThread` i wywołaj `ref.invalidate(messagesProvider)`.
       - Pokaż krótki SnackBar potwierdzający: "Oznaczono jako przeczytana" / "Oznaczono jako nieprzeczytana".
  </action>
  <verify>flutter analyze</verify>
  <acceptance_criteria>
    - Otwarcie nieprzeczytanego wątku automatycznie oznacza go jako przeczytany
    - Przycisk na AppBarze prawidłowo przełącza stan przeczytany/nieprzeczytany
  </acceptance_criteria>
  <done>Użytkownik ma pełną kontrolę nad stanem przeczytania wątku</done>
</task>

<task type="auto">
  <name>Task 3: Dynamiczne liczniki nieprzeczytanych (Badge) na pasku nawigacji, w nagłówku i liście</name>
  <files>
    lib/presentation/screens/main_navigation_screen.dart
    lib/presentation/widgets/app_header.dart
    lib/presentation/screens/messages/messages_screen.dart
  </files>
  <action>
    1. W `lib/presentation/screens/main_navigation_screen.dart`:
       - Oblicz dynamiczny `messageBadgeCount = (messagesAsync.value ?? []).where((m) => m.isUnread).length`.
       - Ustaw `isLabelVisible: messageBadgeCount > 0`.
       - Użyj `AppColors.error` dla badge'a, aby rzucał się w oczy.
    2. W `lib/presentation/widgets/app_header.dart`:
       - Pobieraj liczbę nieprzeczytanych z `messagesProvider` (z fallbackiem do profilu, jeśli lista jeszcze się ładuje).
       - Badge na dzwonku powiadomień ukrywa się, gdy liczba wynosi 0.
    3. W `lib/presentation/screens/messages/messages_screen.dart`:
       - W segmencie tabów napraw badge dla „Wiadomości”: wyświetlaj liczbę TYLKO gdy `unreadMessages > 0` (nigdy `messages.length`!).
       - W kartach wiadomości: wyraźnie wyróżniaj nowe wiadomości (niebieska kropka, pogrubiony tytuł i data, podświetlona ramka).
       - Jeśli `unreadMessages > 0`, dodaj przycisk „Oznacz wszystkie jako przeczytane” obok nagłówka listy.
  </action>
  <verify>flutter analyze</verify>
  <acceptance_criteria>
    - Licznik na dolnym pasku i w nagłówku dokładnie odpowiada liczbie wiadomości z `isUnread: true`
    - Gdy wszystkie wiadomości są przeczytane, badge znika
    - Segment tabów nie pokazuje łącznej sumy wiadomości jako badge
  </acceptance_criteria>
  <done>Wskaźniki liczby nowych wiadomości są spójne i dokładne w całej aplikacji</done>
</task>

</tasks>

<verification>
Automated verification commands:
```bash
/Users/bjankiewicz/flutter/bin/flutter analyze
/Users/bjankiewicz/flutter/bin/flutter build web --release --pwa-strategy=none
```
</verification>
