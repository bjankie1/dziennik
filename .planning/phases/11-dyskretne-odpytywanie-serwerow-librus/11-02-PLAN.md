---
phase: 11-dyskretne-odpytywanie-serwerow-librus
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - functions/src/schedule_evaluator.js
  - functions/index.js
  - functions/test/schedule_window.test.js
  - lib/presentation/providers/sync_provider.dart
  - test/presentation/providers/sync_provider_test.dart
autonomous: true
requirements:
  - REQ-SCHED-01
  - REQ-CLIENT-01
must_haves:
  truths:
    - "schedule_evaluator enforces night silence between 22:30 and 06:30 in Europe/Warsaw timezone per D-01"
    - "schedule_evaluator schedules sync every 30-40 minutes on weekdays during daytime (06:30-16:30) and hourly in evenings per D-02"
    - "schedule_evaluator restricts weekend background sync to 11:00 and 19:00 slots per D-03"
    - "scheduledLibrusSync in index.js runs on 15-minute cadence with evaluator gate, random pre-fetch jitter, and 300s timeout"
    - "sync_provider enforces 120-second client-side cooldown on manual sync clicks with remaining seconds counter per D-04"
    - "sync_provider increases HTTP timeout to 45 seconds to accommodate sequential scraping pacing"
  artifacts:
    - functions/src/schedule_evaluator.js
    - functions/index.js
    - functions/test/schedule_window.test.js
    - lib/presentation/providers/sync_provider.dart
    - test/presentation/providers/sync_provider_test.dart
---

<objective>
Implement Warsaw timezone-aware scheduling with night silence, daytime pacing, weekend restrictions in Cloud Functions, and client-side 120s cooldown with 45s HTTP timeout in the Flutter app, satisfying REQ-SCHED-01, REQ-CLIENT-01, and decisions D-01, D-02, D-03, D-04.

Purpose: Prevent Librus servers from receiving suspicious night-time automated queries, reduce off-peak load, avoid client timeout drops during sequential scraping, and protect manual sync triggers from click-spam.
Output: Warsaw schedule window evaluator `functions/src/schedule_evaluator.js`, updated Cloud Scheduler in `functions/index.js`, client-side cooldown in `lib/presentation/providers/sync_provider.dart`, and unit test suites.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Warsaw Timezone Schedule Window Evaluator and Tests</name>
  <files>
    functions/src/schedule_evaluator.js
    functions/test/schedule_window.test.js
  </files>
  <read_first>
    functions/index.js
    .planning/phases/11-dyskretne-odpytywanie-serwerow-librus/11-CONTEXT.md
    .planning/phases/11-dyskretne-odpytywanie-serwerow-librus/11-RESEARCH.md
  </read_first>
  <action>
    1. Create `functions/src/schedule_evaluator.js`:
       - Export `evaluateScheduleWindow(date = new Date())`:
         - Uses `Intl.DateTimeFormat` with `timeZone: "Europe/Warsaw"` to accurately parse weekday (`Mon`..`Sun`), hour (0..23), and minute (0..59) regardless of host timezone (DST-aware).
         - Calculates `totalMinutes = hour * 60 + minute`.
         - **Cisza nocna (22:30 – 06:30) (D-01):**
           - If `totalMinutes >= 22 * 60 + 30` (>= 1350) OR `totalMinutes < 6 * 60 + 30` (< 390):
           - Return `{ shouldRun: false, reason: "Night silence (22:30 - 06:30)" }`.
         - **Weekendy (Sobota / Niedziela) (D-03):**
           - If `weekday === "Sat" || weekday === "Sun"`:
           - Triggers only in windows 11:00-11:14 or 19:00-19:14:
             - `(hour === 11 && minute < 15) || (hour === 19 && minute < 15)`
             - If true, return `{ shouldRun: true, jitterMaxMs: 180000, reason: `Weekend slot (${hour}:00)` }`.
             - Else return `{ shouldRun: false, reason: "Weekend non-sync window" }`.
         - **Dni powszednie (Poniedziałek – Piątek) (D-02):**
           - **Godziny dzienne (06:30 – 16:30):**
             - `totalMinutes >= 6 * 60 + 30 && totalMinutes < 16 * 60 + 30`
             - In 15-min cadence, runs at `:00` and `:30` windows (`minute < 15 || (minute >= 30 && minute < 45)`):
             - Return `{ shouldRun: true, jitterMaxMs: 240000, reason: "Weekday daytime slot (30-min cadence)" }`.
             - Else return `{ shouldRun: false, reason: "Weekday 15-min offset skip" }`.
           - **Popołudnia / Wieczory (16:30 – 22:30):**
             - `totalMinutes >= 16 * 60 + 30 && totalMinutes < 22 * 60 + 30`
             - In 15-min cadence, runs once per hour at `:00` window (`minute < 15`):
             - Return `{ shouldRun: true, jitterMaxMs: 180000, reason: "Weekday evening slot (60-min cadence)" }`.
             - Else return `{ shouldRun: false, reason: "Weekday evening offset skip" }`.
    2. Create unit tests in `functions/test/schedule_window.test.js`:
       - Night silence: 23:00, 01:30, 04:15, 06:29 -> `shouldRun: false`.
       - Boundary 06:30 -> `shouldRun: true` (daytime slot starts immediately when night silence ends).
       - Boundary 22:30 -> `shouldRun: false` (night silence begins).
       - Weekday daytime: Wed 10:00 -> `true`, Wed 10:15 -> `false`, Wed 10:30 -> `true`.
       - Weekday evening: Thu 17:00 -> `true`, Thu 17:30 -> `false`.
       - Weekend: Sat 11:05 -> `true`, Sat 14:00 -> `false`, Sun 19:10 -> `true`, Sun 20:00 -> `false`.
  </action>
  <acceptance_criteria>
    - `functions/src/schedule_evaluator.js` exists and exports `evaluateScheduleWindow`
    - `evaluateScheduleWindow` returns `{ shouldRun: false }` for 22:30 - 06:30
    - `evaluateScheduleWindow` returns `{ shouldRun: true }` for boundary 06:30 on weekdays
    - `evaluateScheduleWindow` returns `{ shouldRun: true }` for weekend slots at 11:00 and 19:00
    - `node --test functions/test/schedule_window.test.js` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test functions/test/schedule_window.test.js</automated>
    <fails_when>non-zero exit, or "fail" in test output</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Update scheduledLibrusSync in functions/index.js</name>
  <files>
    functions/index.js
  </files>
  <read_first>
    functions/index.js
    functions/src/schedule_evaluator.js
    .planning/phases/11-dyskretne-odpytywanie-serwerow-librus/11-CONTEXT.md
  </read_first>
  <action>
    1. In `functions/index.js`:
       - Import `evaluateScheduleWindow` from `./src/schedule_evaluator`.
       - Update `exports.scheduledLibrusSync`:
         - Change `schedule: "every 30 minutes"` to `schedule: "every 15 minutes"`.
         - Retain `timeZone: "Europe/Warsaw"`.
         - Increase `timeoutSeconds: 300` (5 minutes) and memory `512MiB`.
       - Inside `scheduledLibrusSync` callback:
         ```javascript
         const decision = evaluateScheduleWindow(new Date());
         if (!decision.shouldRun) {
           console.log(`[Scheduler] Sync skipped: ${decision.reason}`);
           return;
         }

         if (decision.jitterMaxMs && decision.jitterMaxMs > 0) {
           const jitter = Math.floor(Math.random() * decision.jitterMaxMs);
           console.log(`[Scheduler] Applying pre-fetch jitter: ${jitter}ms (${decision.reason})`);
           await new Promise(r => setTimeout(r, jitter));
         }

         console.log(`[Scheduler] Starting scheduled sync (${decision.reason})...`);
         const result = await syncStudentData(login, pass);
         ```
  </action>
  <acceptance_criteria>
    - `functions/index.js` contains `schedule: "every 15 minutes"`
    - `functions/index.js` contains `evaluateScheduleWindow`
    - `functions/index.js` contains `timeoutSeconds: 300`
  </acceptance_criteria>
  <verify>
    <automated>node -e "require('./functions/index.js'); console.log('functions/index.js loaded OK')"</automated>
    <fails_when>non-zero exit or exception</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Client-Side 120s Cooldown, 45s HTTP Timeout and Night-Mode Notice in sync_provider.dart</name>
  <files>
    lib/presentation/providers/sync_provider.dart
    test/presentation/providers/sync_provider_test.dart
  </files>
  <read_first>
    lib/presentation/providers/sync_provider.dart
    .planning/phases/11-dyskretne-odpytywanie-serwerow-librus/11-CONTEXT.md
    .planning/phases/11-dyskretne-odpytywanie-serwerow-librus/11-RESEARCH.md
  </read_first>
  <action>
    1. In `lib/presentation/providers/sync_provider.dart`:
       - Increase HTTP call timeouts from 15s/25s to 45 seconds (`Duration(seconds: 45)`) so client does not abort while sequential scraping (18–25s) executes.
       - Add cooldown constant and tracker:
         ```dart
         static const Duration cooldownDuration = Duration(seconds: 120);
         DateTime? _lastSyncTriggerTime;
         ```
       - In `syncNow()`:
         - If `_lastSyncTriggerTime != null`:
           - Calculate `final elapsed = DateTime.now().difference(_lastSyncTriggerTime!);`
           - If `elapsed < cooldownDuration`:
             - `final remaining = (cooldownDuration - elapsed).inSeconds;`
             - `state = state.copyWith(statusMessage: 'Odczekaj jeszcze ${remaining}s przed kolejnym odświeżeniem.');`
             - Return immediately without sending HTTP request.
         - Update `_lastSyncTriggerTime = DateTime.now();`
         - Check night mode:
           - Helper `bool _isNightSilence(DateTime dt) { final m = dt.hour * 60 + dt.minute; return m >= 22 * 60 + 30 || m < 6 * 60 + 30; }`
           - If sync succeeds during night hours, format statusMessage: `'Dane zaktualizowane na żądanie (serwery w trybie nocnym)'`.
    2. Create unit test `test/presentation/providers/sync_provider_test.dart`:
       - Test that consecutive calls to `syncNow()` within 120s are throttled with cooldown status message.
       - Test that after cooldown expires, sync is permitted.
  </action>
  <acceptance_criteria>
    - `lib/presentation/providers/sync_provider.dart` uses `Duration(seconds: 45)` for HTTP timeout
    - `lib/presentation/providers/sync_provider.dart` contains `cooldownDuration` of 120 seconds
    - `lib/presentation/providers/sync_provider.dart` checks remaining seconds and sets statusMessage
    - `flutter test test/presentation/providers/sync_provider_test.dart` exits 0
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/presentation/providers/sync_provider_test.dart</automated>
    <fails_when>non-zero exit, or "Some tests failed"</fails_when>
  </verify>
</task>

</tasks>
