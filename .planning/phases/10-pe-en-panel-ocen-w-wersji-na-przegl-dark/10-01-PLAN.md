---
phase: 10-pe-en-panel-ocen-w-wersji-na-przegl-dark
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/theme/app_colors.dart
  - lib/presentation/providers/school_providers.dart
  - lib/presentation/screens/grades/grades_screen.dart
  - lib/presentation/screens/grades/widgets/academic_kpi_row.dart
  - lib/presentation/screens/grades/widgets/weighted_average_kpi_card.dart
  - lib/presentation/screens/grades/widgets/grade_distribution_card.dart
  - lib/presentation/screens/grades/widgets/subject_inspector_card.dart
  - lib/presentation/screens/grades/widgets/subject_ledger_table.dart
autonomous: true
requirements:
  - REQ-GRADES-05
  - REQ-GRADES-06
must_haves:
  truths:
    - "AppColors defines primaryFixedDim (0xFFC3C0FF) and tertiaryFixedDim (0xFFFFB77D)"
    - "Riverpod 3.x Notifiers exist for selectedGradesSubjectProvider (Subject?, null default), gradesSearchQueryProvider (String), gradesTermProvider (int), and gradesDistributionStatsProvider"
    - "WeightedAverageKpiCard displays weighted average score (4.82), trend pill (+0.14), and class rank ('Top 5% w klasie 3B (2. lokata na 28 uczniów)') per D-09"
    - "GradeDistributionCard displays MEN 1-6 scale bar histogram with safety badge ('0 zagrożeń • 100% pozytywnych') per D-08"
    - "SubjectInspectorCard renders clean empty state when no subject is selected ('Wybierz przedmiot z tabeli po lewej stronie...') per D-03, and detailed inspector when subject is selected per D-04"
    - "SubjectLedgerTable renders 8-col table with search input, weights legend, subject avatar & level, wrap of grade pills, average, predicted grade, and active row highlighting (4px left primary bar) per D-04"
    - "Clicking a grade pill inside SubjectLedgerTable invokes onTapGrade without triggering row selection per D-05"
    - "GradesScreen switches responsively at 1024px: Master-Detail 8+4 grid on desktop vs single-column mobile view with accordions per D-01 and D-02"
  artifacts:
    - lib/core/theme/app_colors.dart
    - lib/presentation/providers/school_providers.dart
    - lib/presentation/screens/grades/widgets/academic_kpi_row.dart
    - lib/presentation/screens/grades/widgets/weighted_average_kpi_card.dart
    - lib/presentation/screens/grades/widgets/grade_distribution_card.dart
    - lib/presentation/screens/grades/widgets/subject_inspector_card.dart
    - lib/presentation/screens/grades/widgets/subject_ledger_table.dart
    - lib/presentation/screens/grades/grades_screen.dart
---

<objective>
Implement the architectural foundation, Riverpod 3.x state providers, design system tokens, Academic KPI banner cards, Master Ledger Table, Detail Inspector Card, and Master-Detail responsive shell in `GradesScreen` satisfying REQ-GRADES-05, REQ-GRADES-06, and decisions D-01, D-02, D-03, D-04, D-05, D-08, D-09.

Purpose: Provide a high-density, academic Master-Detail experience on desktop (>=1024px) with reactive subject selection, search filtering, and grade distribution analytics, while keeping mobile/tablet (<1024px) fully functional.
Output: Design tokens in `AppColors`, Riverpod 3.x providers in `school_providers.dart`, 5 new widget components in `lib/presentation/screens/grades/widgets/`, and updated `GradesScreen`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Design Tokens in AppColors and Riverpod 3.x State Providers</name>
  <files>
    lib/core/theme/app_colors.dart
    lib/presentation/providers/school_providers.dart
  </files>
  <action>
    1. In `lib/core/theme/app_colors.dart`:
       - Add missing color tokens from `docs/panel ocen/DESIGN.md`:
         ```dart
         static const Color primaryFixedDim = Color(0xFFC3C0FF);
         static const Color tertiaryFixedDim = Color(0xFFFFB77D);
         ```
    2. In `lib/presentation/providers/school_providers.dart`:
       - Define `SelectedGradesSubjectNotifier` adhering to Riverpod 3.x `Notifier<Subject?>`:
         ```dart
         class SelectedGradesSubjectNotifier extends Notifier<Subject?> {
           @override
           Subject? build() => null; // D-03: default null / empty

           void select(Subject? subject) {
             if (state?.id == subject?.id) {
               state = null; // toggle off
             } else {
               state = subject;
             }
           }

           void clear() => state = null;
         }

         final selectedGradesSubjectProvider =
             NotifierProvider<SelectedGradesSubjectNotifier, Subject?>(SelectedGradesSubjectNotifier.new);
         ```
       - Define `GradesSearchQueryNotifier` adhering to Riverpod 3.x `Notifier<String>`:
         ```dart
         class GradesSearchQueryNotifier extends Notifier<String> {
           @override
           String build() => '';

           void setQuery(String query) => state = query;
           void clear() => state = '';
         }

         final gradesSearchQueryProvider =
             NotifierProvider<GradesSearchQueryNotifier, String>(GradesSearchQueryNotifier.new);
         ```
       - Define `GradesTermNotifier` adhering to Riverpod 3.x `Notifier<int>`:
         ```dart
         class GradesTermNotifier extends Notifier<int> {
           @override
           int build() => 1; // 1 = Semestr 1, 2 = Semestr 2, 3 = Roczna

           void setTerm(int term) => state = term;
         }

         final gradesTermProvider =
             NotifierProvider<GradesTermNotifier, int>(GradesTermNotifier.new);
         ```
       - Define `GradesDistributionStats` model & `gradesDistributionStatsProvider`:
         - Aggregates grades from `subjectsProvider` for the active term.
         - Buckets grades into MEN scale 1..6 based on `numericValue.round().clamp(1, 6)`.
         - Calculates: `counts` (`Map<int, int>`), `totalGrades`, `hasThreats` (count of 1s > 0), `dangerCount` (1s and 2s), `positivePercentage` (percentage of grades >= 2).
  </action>
  <verify>flutter analyze lib/core/theme/app_colors.dart lib/presentation/providers/school_providers.dart</verify>
  <acceptance_criteria>
    - `AppColors.primaryFixedDim` and `AppColors.tertiaryFixedDim` are defined.
    - `selectedGradesSubjectProvider` initializes to `null` (D-03).
    - `gradesSearchQueryProvider` and `gradesTermProvider` use Riverpod 3.x `NotifierProvider`.
    - `flutter analyze` passes without errors or warnings.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Academic KPI Row & Subject Inspector Card</name>
  <files>
    lib/presentation/screens/grades/widgets/weighted_average_kpi_card.dart
    lib/presentation/screens/grades/widgets/grade_distribution_card.dart
    lib/presentation/screens/grades/widgets/academic_kpi_row.dart
    lib/presentation/screens/grades/widgets/subject_inspector_card.dart
  </files>
  <action>
    1. Create `lib/presentation/screens/grades/widgets/weighted_average_kpi_card.dart`:
       - Visual design matching `docs/panel ocen/screen.png` and `docs/panel ocen/DESIGN.md`:
         - Card with 4px top accent line (`AppColors.primary`).
         - Top label: "ŚREDNIA WAŻONA • SEMESTR 1" with `school_rounded` or `calculate_outlined` icon.
         - Large numeric average display: 46px bold `overallAverage` (e.g. `4.82`).
         - Trend pill next to average: `trending_up +0.14` in `Color(0xFFDCFCE7)` badge with `Color(0xFF15803D)` text.
         - Subtitle: "Wyliczona na podstawie 38 ocen cząstkowych".
         - Footer badge: `Top 5% w klasie 3B (2. lokata na 28 uczniów)` with `stars_rounded` icon in `AppColors.secondary` (D-09).
    2. Create `lib/presentation/screens/grades/widgets/grade_distribution_card.dart`:
       - Visual design matching `docs/panel ocen/screen.png` (D-08):
         - Card with 4px top accent line (`AppColors.primaryContainer`).
         - Top row: title "ROZKŁAD OCEN CZĄSTKOWYCH (SKALA 1–6)", total count badge (e.g. `38 ocen wpisanych w semestrze`), and safety badge:
           - Safety badge: `0 zagrożeń • 100% pozytywnych` with `verified` icon in `Color(0xFFDCFCE7)`.
         - Bar histogram for grades 1..6:
           - 6 columns with labels 1, 2, 3, 4, 5, 6 and counts on top of bars.
           - Bar heights scaled relative to maximum count in any bucket (min height 8px for 0, up to 72px for max).
           - Bar colors:
             - 1 & 2: `AppColors.surfaceContainerHigh` if 0, `AppColors.error` if > 0.
             - 3: `AppColors.tertiaryFixedDim` (amber).
             - 4: `AppColors.primaryFixedDim` (light indigo).
             - 5: `AppColors.primaryContainer` (deep indigo).
             - 6: `AppColors.secondary` (emerald).
         - Legend footer: percentages and labels (np. `Niedostateczne (0%) • Dopuszczające (0%) • Dostateczne (7.9%) • Dobre (23.7%) • B. dobre (47.4%) • Celujące (21.1%)`).
    3. Create `lib/presentation/screens/grades/widgets/academic_kpi_row.dart`:
       - Combines `WeightedAverageKpiCard` (flex 4 or 380px) and `GradeDistributionCard` (flex 8) in a responsive `Row` (or `Column` if width < 1024px).
    4. Create `lib/presentation/screens/grades/widgets/subject_inspector_card.dart`:
       - Receives `Subject? subject` and `void Function(Grade grade, Subject subject)? onTapGrade`.
       - **Empty State (D-03)**:
         - When `subject == null`:
         - Centered placeholder inside styled container:
           - Icon: `touch_app_outlined` or `info_outline` (44px, `AppColors.outlineVariant`).
           - Heading: "Brak wybranego przedmiotu" (16px, FontWeight.w700).
           - Description: "Wybierz przedmiot z tabeli po lewej stronie, aby wyświetlić szczegółowy rejestr ocen i statystyki." (13px, `AppColors.onSurfaceVariant`).
       - **Populated State (D-04)**:
         - When `subject != null`:
         - Header: Subject title, level pill (`Rozszerzony`), teacher name, weighted average badge (`4.90`), and count of grades.
         - List of grades with card items:
           - Grade badge circle (`5`, `4+`, etc.).
           - Title/topic of the grade, category pill (`Odpowiedź ustna`, `Sprawdzian`), weight (`w:3`), date (`15 września`).
           - Teacher comment quote preview (if available).
           - Tapping on a grade card invokes `onTapGrade(grade, subject)`.
         - EduSync AI Study Tip box:
           - Light purple container with `psychology` icon: "Wskazówka EduSync AI: Utrzymaj obecną średnią z kartkówek – kolejna ocena 5 zagwarantuje ocenę celującą na koniec semestru."
         - Quick action buttons:
           - "Symuluj ocenę dla tego przedmiotu" (opens simulator).
           - "Napisz do nauczyciela" (message teacher).
  </action>
  <verify>flutter analyze lib/presentation/screens/grades/widgets/</verify>
  <acceptance_criteria>
    - `WeightedAverageKpiCard`, `GradeDistributionCard`, `AcademicKpiRow`, and `SubjectInspectorCard` compile without errors.
    - Empty state message exactly matches D-03 specification.
    - Safety badge and MEN 1-6 histogram match D-08 specification.
    - Weighted average score, trend pill, and class rank match D-09 specification.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: SubjectLedgerTable & Master-Detail Shell Integration in GradesScreen</name>
  <files>
    lib/presentation/screens/grades/widgets/subject_ledger_table.dart
    lib/presentation/screens/grades/grades_screen.dart
  </files>
  <action>
    1. Create `lib/presentation/screens/grades/widgets/subject_ledger_table.dart`:
       - Header strip:
         - Search text box (`gradesSearchQueryProvider`): "Szukaj przedmiotu lub nauczyciela...".
         - Weights legend chips:
           - `Waga 3 (Sprawdzian)`: `Color(0xFFFEE2E2)` / `Color(0xFFDC2626)`.
           - `Waga 2 (Kartkówka)`: `Color(0xFFEFF4FF)` / `Color(0xFF4F46E5)`.
           - `Waga 1 (Bieżąca)`: `Color(0xFFF1F5F9)` / `Color(0xFF64748B)`.
       - Table header:
         - "PRZEDMIOT I NAUCZYCIEL"
         - "OCENY BIEŻĄCE (WAGA)"
         - "ŚREDNIA"
         - "PRZEWIDYWANA"
         - "OSTATNI WPIS"
       - Subject rows:
         - Subject icon avatar (mathematical/subject glyphs: ∑, λ, ⚗, 📖, 🌍, 💻, etc.), name, level tag (`Rozszerz.`), teacher name.
         - Grade pills wrap:
           - Styled pills (`5 w:3`, `4+ w:2`) with weight color coding.
           - Clicking a pill invokes `onTapGrade(grade, subject)` and stops propagation / does NOT select row (D-05).
         - Weighted average badge (e.g. `4.90` with subtle background).
         - Predicted final grade badge (e.g. `5` in green/emerald pill).
         - Last entry date and teacher initials.
         - Row selection styling (D-04):
           - When `subject == selectedSubject`: 4px left border in `AppColors.primary`, background in `AppColors.surfaceContainerHigh.withOpacity(0.4)`.
           - Row tap calls `onSelectSubject(subject)`.
       - Table summary footer:
         - Total count: e.g. "14 przedmiotów zrealizowanych w semestrze zimowym".
         - Overall weighted average display badge.
    2. Refactor `lib/presentation/screens/grades/grades_screen.dart`:
       - Add responsive check with `LayoutBuilder`:
         ```dart
         final isDesktop = constraints.maxWidth >= 1024; // D-01
         ```
       - On **Desktop (>=1024px)**:
         - Top Academic Breadcrumb & Action Toolbar:
           - Breadcrumb: "Dziennik VULCAN • Klasa 3B (Profil Mat-Fiz) • Rok szkolny 2024/2025".
           - Action buttons: "Filtruj wg wag", "Eksportuj (PDF/XLS)", "Drukuj", "Przelicz GPA" (opens `AverageSimulatorModal`).
         - Term Switcher: Tabs for "Semestr 1", "Semestr 2", "Klasyfikacja Roczna" connected to `gradesTermProvider`.
         - `AcademicKpiRow` (Weighted Average + Grade Distribution cards).
         - 12-column Grid:
           - Left 8 columns:
             - `SubjectLedgerTable` (with subject selection and grade tap handlers).
             - Placeholder container for `AverageTrajectoryCard` (to be connected in Plan 10-02).
           - Right 4 columns:
             - `SubjectInspectorCard` (observing `selectedGradesSubjectProvider`).
       - On **Mobile (<1024px)**:
         - Preserves existing single-column view with touch-friendly cards, inline pills, and expandable accordions (D-02).
  </action>
  <verify>flutter analyze lib/presentation/screens/grades/</verify>
  <acceptance_criteria>
    - `SubjectLedgerTable` renders all 8 columns with search, weights legend, and summary footer.
    - Row click toggles selection and updates right inspector card (D-04).
    - Grade pill tap invokes `onTapGrade` without changing row selection (D-05).
    - `GradesScreen` renders desktop 8+4 Master-Detail on >=1024px (D-01) and preserves mobile single-column view on <1024px (D-02).
    - `flutter analyze` passes with 0 errors.
  </acceptance_criteria>
</task>

</tasks>
