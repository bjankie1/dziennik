---
phase: 09-plan-lekcji-w-wersji-web-widok-siatki-i-agendy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/domain/models/lesson_slot.dart
  - lib/data/mock/mock_data.dart
  - lib/data/repositories/school_repository.dart
  - lib/data/repositories/mock_school_repository.dart
  - lib/data/repositories/firestore_school_repository.dart
  - lib/presentation/providers/school_providers.dart
autonomous: true
requirements:
  - REQ-TIMETABLE-01
  - REQ-TIMETABLE-04
must_haves:
  truths:
    - "Model LessonSlot posiada opcjonalne pola: topic, homework, materials, eventType, eventTitle"
    - "MockData zawiera pełny tygodniowy plan lekcji (Poniedziałek - Piątek) z realistycznymi przedmiotami, salami, nauczycielami, tematami, zastępstwami, odwołaniami i sprawdzianami zgodnymi z makietami"
    - "SchoolRepository, MockSchoolRepository oraz FirestoreSchoolRepository udostępniają metodę getWeekSchedule() zwracającą plan dla dni 1..5"
    - "Istnieją providerzy Riverpod: weekScheduleProvider, selectedScheduleDayProvider, weekScheduleFilterProvider oraz weeklyStatsProvider obliczający statystyki tygodnia"
  artifacts:
    - lib/domain/models/lesson_slot.dart
    - lib/data/mock/mock_data.dart
    - lib/data/repositories/school_repository.dart
    - lib/data/repositories/mock_school_repository.dart
    - lib/data/repositories/firestore_school_repository.dart
    - lib/presentation/providers/school_providers.dart
---

<objective>
Rozszerzenie warstwy modeli, danych i providerów (Data & State Layer) dla planu lekcji: dodanie pól tematu, materiałów, zadań i sprawdzianów do LessonSlot, przygotowanie pełnego tygodniowego planu zajęć w MockData oraz wdrożenie providerów Riverpod dla tygodniowej siatki i statystyk.

Purpose: Zapewnienie kompletnego, spójnego modelu i stanu wymaganego przez widok Siatki oraz widok Agendy.
Output: Rozszerzony LessonSlot, zaktualizowane repozytoria, bogaty tygodniowy zestaw danych i nowe providery Riverpod.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Rozszerzenie modelu LessonSlot o dodatkowe metadane</name>
  <files>
    lib/domain/models/lesson_slot.dart
  </files>
  <action>
    W pliku `lib/domain/models/lesson_slot.dart`:
    1. Dodaj do klasy `LessonSlot` opcjonalne pola:
       - `final String? topic;` (temat lekcji)
       - `final String? homework;` (zadanie domowe)
       - `final String? materials;` (materiały / podręcznik)
       - `final String? eventType;` (np. 'Sprawdzian', 'Kartkówka')
       - `final String? eventTitle;` (tytuł sprawdzianu, np. 'Reakcje redox')
    2. Zaktualizuj konstruktor `const LessonSlot(...)`, zachowując kompatybilność wsteczną (wszystkie nowe pola są opcjonalne).
  </action>
  <verify>flutter analyze</verify>
  <acceptance_criteria>
    - `LessonSlot` kompiluje się bez błędów.
    - Pola `topic`, `homework`, `materials`, `eventType`, `eventTitle` są dostępne w instancjach `LessonSlot`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Przygotowanie pełnego tygodniowego planu w MockData i repozytoriach</name>
  <files>
    lib/data/mock/mock_data.dart
    lib/data/repositories/school_repository.dart
    lib/data/repositories/mock_school_repository.dart
    lib/data/repositories/firestore_school_repository.dart
  </files>
  <action>
    1. W `lib/data/mock/mock_data.dart`:
       - Utwórz `static final Map<int, List<LessonSlot>> weekSchedule`, zawierający plan dla dni 1 do 5 (Poniedziałek – Piątek):
         - Poniedziałek: Język Polski (s. 204, temat: "Pan Tadeusz"), Matematyka R (s. 212), J. Angielski (s. 305), Historia (s. 112), Biologia (s. Bio 1), WF.
         - Wtorek: Geografia (Zastępstwo za mgr Wiśniewskiego, s. 102), Chemia, Fizyka, J. Polski, Matematyka, Informatyka.
         - Środa: J. Angielski, Matematyka, J. Polski, Biologia, Chemia, Historia.
         - Czwartek: 1. Fizyka (status: canceled, statusNote: "Zwolnienie lekarskie nauczyciela"), 2. Język Polski (status: inProgress, progressFraction: 0.65, topic: "Analiza aktu III dramatu Dziady cz. III", materials: "Podręcznik s. 142–148", homework: "Konspekt rozprawki na wybrany temat"), 3. Matematyka R (status: substituted, statusNote: "Zastępstwo: mgr M. Kowalczyk • Zmiana sali na 301", originalRoom: "Sala 212", room: "Sala 301"), 4. Chemia (eventType: "Sprawdzian", eventTitle: "Reakcje redox"), 5. J. Angielski, 6. Biologia.
         - Piątek: Historia, Język Polski (eventType: "Sprawdzian", eventTitle: "Romantyzm"), Matematyka, J. Niemiecki, WF, Godzina Wychowawcza.
    2. W `lib/data/repositories/school_repository.dart`:
       - Dodaj sygnaturę metody: `Future<Map<int, List<LessonSlot>>> getWeekSchedule({DateTime? weekStart});`
    3. W `lib/data/repositories/mock_school_repository.dart`:
       - Zaimplementuj `getWeekSchedule({DateTime? weekStart})`, zwracając `MockData.weekSchedule`.
       - Zaktualizuj `getScheduleForDay(int dayOfWeek)`, zwracając `MockData.weekSchedule[dayOfWeek] ?? MockData.todaySchedule`.
    4. W `lib/data/repositories/firestore_school_repository.dart`:
       - Zaimplementuj `getWeekSchedule({DateTime? weekStart})`, parsując dni 1..5 z pobranego timetable i łącząc je ze sprawdzianami z terminarza (gdy pasują do przedmiotu i daty).
  </action>
  <verify>flutter analyze</verify>
  <acceptance_criteria>
    - `SchoolRepository`, `MockSchoolRepository` i `FirestoreSchoolRepository` implementują `getWeekSchedule()`.
    - `flutter analyze` kończy się kodem 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Utworzenie providerów Riverpod dla tygodniowego planu i filtrów</name>
  <files>
    lib/presentation/providers/school_providers.dart
  </files>
  <action>
    W `lib/presentation/providers/school_providers.dart`:
    1. Dodaj `weekScheduleProvider`:
       ```dart
       final weekScheduleProvider = FutureProvider<Map<int, List<LessonSlot>>>((ref) async {
         final repo = ref.watch(schoolRepositoryProvider);
         return repo.getWeekSchedule();
       });
       ```
    2. Dodaj `weekScheduleFilterProvider`:
       ```dart
       enum WeekScheduleFilter { none, substitutions, exams, canceled }
       final weekScheduleFilterProvider = StateProvider<WeekScheduleFilter>((ref) => WeekScheduleFilter.none);
       ```
    3. Dodaj `selectedScheduleDayProvider` do synchronizacji wybranego dnia pomiędzy widokami:
       ```dart
       final selectedScheduleDayProvider = StateProvider<int>((ref) {
         final now = DateTime.now();
         return (now.weekday - 1).clamp(0, 4); // 0=Pn .. 4=Pt
       });
       ```
    4. Dodaj `weeklyScheduleStatsProvider`:
       Obliczający:
       - `totalHours`: łączna liczba lekcji w tygodniu
       - `substitutionsCount`: liczba lekcji ze statusem `substituted`
       - `examsCount`: liczba lekcji ze sprawdzianami (`eventType != null`)
       - `canceledCount`: liczba lekcji ze statusem `canceled`
  </action>
  <verify>flutter analyze</verify>
  <acceptance_criteria>
    - Providery `weekScheduleProvider`, `weekScheduleFilterProvider`, `selectedScheduleDayProvider` i `weeklyScheduleStatsProvider` są zdefiniowane i eksportowane.
    - `flutter analyze` nie zgłasza żadnych błędów.
  </acceptance_criteria>
</task>

</tasks>
