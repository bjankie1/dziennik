---
phase: 10-pe-en-panel-ocen-w-wersji-na-przegl-dark
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - lib/presentation/screens/grades/widgets/trajectory_chart_painter.dart
  - lib/presentation/screens/grades/widgets/average_trajectory_card.dart
  - lib/presentation/screens/grades/widgets/grade_details_side_sheet.dart
  - lib/presentation/screens/grades/grade_details_modal.dart
  - lib/presentation/screens/grades/grades_screen.dart
autonomous: true
requirements:
  - REQ-GRADES-07
  - REQ-GRADES-08
must_haves:
  truths:
    - "TrajectoryChartPainter draws smooth cubic Bézier spline with gradient area fill under the line and dashed class average reference line per D-10"
    - "AverageTrajectoryCard embeds the spline chart canvas with historical checkpoints (01 Wrz -> Dzisiaj) and class average comparison (4.18)"
    - "GradeDetailsSideSheet displays on desktop (>=1024px) as slide-in drawer (~500px wide) with dimmed backdrop scrim and ESC/close dismissal per D-06, and as bottom sheet on mobile"
    - "GradeDetailsSideSheet contains Hero grade badge, metadata register, teacher quote box, and dynamic GPA impact calculator ('+0.06 pkt, Przed -> Teraz') per D-07"
    - "Tapping a grade pill in SubjectLedgerTable opens GradeDetailsSideSheet without changing selectedGradesSubjectProvider in the inspector per D-05"
    - "All 'Przelicz GPA' actions invoke AverageSimulatorModal"
    - "flutter analyze passes with zero errors and warnings"
  artifacts:
    - lib/presentation/screens/grades/widgets/trajectory_chart_painter.dart
    - lib/presentation/screens/grades/widgets/average_trajectory_card.dart
    - lib/presentation/screens/grades/widgets/grade_details_side_sheet.dart
    - lib/presentation/screens/grades/grade_details_modal.dart
    - lib/presentation/screens/grades/grades_screen.dart
---

<objective>
Implement the Average Trajectory Spline Chart with CustomPainter and Bézier curve, the Grade Details Side Sheet / Drawer with dynamic GPA impact visualizer and quotation box, integrate grade pill click delegation, and verify complete system stability with `flutter analyze`.

Purpose: Fulfill REQ-GRADES-07 and REQ-GRADES-08 with full adherence to decisions D-05, D-06, D-07, and D-10, completing the Phase 10 Academic Portal.
Output: `TrajectoryChartPainter`, `AverageTrajectoryCard`, `GradeDetailsSideSheet`, backward-compatible `grade_details_modal.dart`, and fully wired `grades_screen.dart`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Average Trajectory Spline Chart (CustomPainter & Card)</name>
  <files>
    lib/presentation/screens/grades/widgets/trajectory_chart_painter.dart
    lib/presentation/screens/grades/widgets/average_trajectory_card.dart
  </files>
  <action>
    1. Create `lib/presentation/screens/grades/widgets/trajectory_chart_painter.dart`:
       - Define data model `TrajectoryPoint` (label: e.g. "01 Wrz", value: double e.g. 4.60, isLatest: bool).
       - Create `TrajectoryChartPainter extends CustomPainter`:
         - Fields: `List<TrajectoryPoint> points`, `double classAverage` (e.g. 4.18), `double minY` (e.g. 3.5), `double maxY` (e.g. 5.2), `Color primaryColor`, `Color classAvgColor`.
         - Paint steps:
           1. Draw horizontal subtle dashed grid lines at 4.0, 4.5, 5.0 with value labels on the left.
           2. Draw dashed reference line for `classAverage` (4.18) with a tag/pill "Średnia klasy: 4.18".
           3. Build smooth cubic Bézier path (`Path.cubicTo`) through all `points`:
              - Compute control points between $(x_i, y_i)$ and $(x_{i+1}, y_{i+1})$ using smooth tangent interpolation for a natural academic spline.
           4. Fill gradient area under the curve:
              - Extend path to bottom corners `(width, height) -> (0, height)`.
              - Apply `LinearGradient(begin: Alignment.topCenter, end: Alignment.bottomCenter, colors: [primaryColor.withOpacity(0.18), primaryColor.withOpacity(0.0)])`.
           5. Stroke the main spline line:
              - Paint with `strokeWidth: 3.0`, `color: primaryColor`, `strokeCap: StrokeCap.round`, `strokeJoin: StrokeJoin.round`.
           6. Draw measurement dots at point coordinates:
              - Standard points: white filled circle with 2px `primaryColor` border (radius 5).
              - Latest point (`isLatest == true`): solid circle with pulsing glow / larger radius (radius 6.5).
         - `shouldRepaint`: compare points and classAverage.
    2. Create `lib/presentation/screens/grades/widgets/average_trajectory_card.dart`:
       - Card styled with `AppColors.surfaceContainerLowest` and subtle border `AppColors.surfaceContainerHigh`.
       - Header:
         - Title: "TRAJEKTORIA ŚREDNIEJ W SEMESTRZE"
         - Subtitle: "Dynamika ocen na osi czasu vs średnia klasy (4.18)"
         - Legend pills: "Twoja średnia (4.82)" (solid indigo line indicator) and "Średnia klasy (4.18)" (dashed gray line indicator).
       - Spline canvas:
         - `CustomPaint(size: Size(double.infinity, 160), painter: TrajectoryChartPainter(...))`.
       - X-Axis checkpoint labels row:
         - Labels aligned under the points: "01 Wrz (4.60)", "15 Wrz (4.68)", "01 Paź (4.74)", "15 Paź (4.78)", "Dzisiaj (4.82)".
       - Footer stats summary:
         - Rate of change pill: "+0.22 pkt od początku semestru • Stały trend wzrostowy".
  </action>
  <verify>flutter analyze lib/presentation/screens/grades/widgets/trajectory_chart_painter.dart lib/presentation/screens/grades/widgets/average_trajectory_card.dart</verify>
  <acceptance_criteria>
    - `TrajectoryChartPainter` draws cubic Bézier spline, area gradient fill, and dashed class average line (D-10).
    - `AverageTrajectoryCard` presents chart with aligned X-axis labels and legend indicators.
    - `flutter analyze` passes without errors.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Grade Details Side Sheet / Drawer & Dynamic GPA Impact</name>
  <files>
    lib/presentation/screens/grades/widgets/grade_details_side_sheet.dart
    lib/presentation/screens/grades/grade_details_modal.dart
  </files>
  <action>
    1. Create `lib/presentation/screens/grades/widgets/grade_details_side_sheet.dart`:
       - Implement static presentation method `GradeDetailsSideSheet.show(BuildContext context, {required Grade grade, required Subject subject})`:
         - Desktop (>=1024px, D-06):
           - Invokes `showGeneralDialog(...)`.
           - `barrierDismissible: true`, `barrierColor: Color(0x66213145)` (dimmed scrim).
           - `transitionDuration: Duration(milliseconds: 250)`.
           - `transitionBuilder`: `SlideTransition` from `Offset(1.0, 0.0)` to `Offset.zero` with `Curves.easeOutCubic`.
           - Renders right-aligned sheet: `SizedBox(width: 500, height: double.infinity, child: GradeDetailsSideSheet(...))`.
           - Dismissible via ESC key (`Actions` / `Shortcuts` or default `ModalRoute`), close (X) button, or backdrop tap.
         - Mobile (<1024px, D-06):
           - Invokes `showModalBottomSheet(...)` with `isScrollControlled: true`.
       - Content structure matching `docs/szczegóły oceny/` (D-07):
         1. Header:
            - Subject and class tag: e.g. "${subject.name} • Klasa 3B".
            - Title: "Szczegóły oceny cząstkowej".
            - Close (X) icon button.
         2. Hero Grade Banner:
            - Large badge: `${grade.rawValue} ${grade.verbalGrade}` (e.g. `5 Bardzo dobra`).
            - Category pill (e.g. `Odpowiedź ustna`) and weight pill (`Waga klasyfikacyjna: ${grade.weight}`).
            - Topic title: `grade.topic` or default assignment title.
            - Date & time string (e.g. `Wtorek, 15 września 2024 r., 11:40`).
         3. Dynamic Average Impact Visualizer (D-07):
            - Computes average before this grade vs average with this grade:
              $$\Delta = \text{avgAfter} - \text{avgBefore}$$
            - Impact pill: `WPŁYW NA ŚREDNIĄ: +0.06 pkt` (or neutral/negative).
            - Sub-label: `Średnia przed: 4.88 → po: 4.92 (+0.04)`.
            - Progress bar displaying position between 1.00 and 6.00 with emerald fill.
         4. Karta Ewidencyjna Oceny (MEN Key-Value Grid):
            - Ocena cyfrowa: `${grade.rawValue} (${grade.verbalGrade} • ${grade.percentage ?? 100}%)`.
            - Kategoria: `${grade.category}`.
            - Data lekcji: `${grade.date}`.
            - Przedmiot: `${subject.name}`.
            - Nauczyciel & Dodał: `${subject.teacherName}`.
            - Waga oceny: `${grade.weight} (${'★' * grade.weight})`.
            - Licz do średniej: badge "TAK (Wliczana)".
         5. Teacher Quotation Box:
            - Styled italic comment with 4px left primary accent border and subtle background:
              `"${grade.comment.isNotEmpty ? grade.comment : 'Świetna wypowiedź, bezbłędna terminologia naukowa i logiczny wywód.'}"`
            - Teacher signature: `${subject.teacherName}`.
            - Evaluated competencies pills: `Wiedza merytoryczna`, `Precyzja językowa`, `Argumentacja`.
         6. Action Buttons:
            - Primary button: "Napisz do nauczyciela" with `mail_outline` icon.
            - Secondary button: "Zapisz się na konsultacje / Zgłoś zapytanie" with `event_available` icon.
            - Auxiliary button: "Pobierz wyciąg ocen (PDF)".
    2. Update `lib/presentation/screens/grades/grade_details_modal.dart`:
       - Refactor to maintain 100% backward compatibility:
         - Keep `GradeDetailsModal.show(context, grade, {subject})` but delegate internally to `GradeDetailsSideSheet.show`.
  </action>
  <verify>flutter analyze lib/presentation/screens/grades/widgets/grade_details_side_sheet.dart lib/presentation/screens/grades/grade_details_modal.dart</verify>
  <acceptance_criteria>
    - `GradeDetailsSideSheet` implements slide-in drawer on desktop (>=1024px) and bottom sheet on mobile (D-06).
    - Hero badge, metadata register, quotation box, and dynamic GPA impact calculator match D-07.
    - Existing calls to `GradeDetailsModal.show` continue to compile and work.
    - `flutter analyze` passes with zero errors.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Full End-to-End Wiring and Verification</name>
  <files>
    lib/presentation/screens/grades/grades_screen.dart
  </files>
  <action>
    1. In `lib/presentation/screens/grades/grades_screen.dart`:
       - Embed `AverageTrajectoryCard` in the desktop left column right below `SubjectLedgerTable`.
       - Connect `SubjectLedgerTable` grade pill taps:
         - When user clicks a pill in the table: invoke `GradeDetailsSideSheet.show(context, grade: grade, subject: subject)` (D-05).
         - Ensure the row's selection state (`selectedGradesSubjectProvider`) is NOT modified when tapping a pill (D-05).
       - Connect `SubjectInspectorCard` grade card taps:
         - When user clicks a grade in the inspector: invoke `GradeDetailsSideSheet.show(context, grade: grade, subject: subject)`.
       - Connect "Przelicz GPA" buttons (in desktop toolbar, mobile header icon, and inspector card) to `AverageSimulatorModal.show(context, cleanSubjects, overallAvg)`.
       - Connect "Filtruj wg wag" toolbar button to toggle or focus search.
       - Ensure responsive switching behaves properly across 1024px boundary without render overflow errors.
    2. Run full static verification:
       - Run `flutter analyze` across the entire project.
       - Fix any unused imports, deprecations, or formatting issues.
  </action>
  <verify>flutter analyze</verify>
  <acceptance_criteria>
    - `AverageTrajectoryCard` is visible on desktop below `SubjectLedgerTable`.
    - Clicking a grade pill anywhere opens `GradeDetailsSideSheet` without interfering with row selection (D-05).
    - "Przelicz GPA" opens `AverageSimulatorModal`.
    - `flutter analyze` exits with code 0 (no errors, no warnings).
  </acceptance_criteria>
</task>

</tasks>
