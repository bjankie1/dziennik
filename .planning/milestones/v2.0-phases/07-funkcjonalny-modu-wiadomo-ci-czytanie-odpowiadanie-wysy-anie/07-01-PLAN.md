---
phase: 07-funkcjonalny-modu-wiadomo-ci-czytanie-odpowiadanie-wysy-anie
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/domain/models/message_thread.dart
  - lib/domain/models/teacher_contact.dart
  - lib/data/repositories/school_repository.dart
  - lib/data/repositories/mock_school_repository.dart
  - lib/data/repositories/firestore_school_repository.dart
  - lib/presentation/providers/school_providers.dart
  - lib/presentation/screens/messages/messages_screen.dart
  - lib/presentation/screens/messages/message_thread_screen.dart
  - lib/presentation/screens/messages/new_message_screen.dart
  - functions/src/librus_client.js
  - functions/index.js
autonomous: true
requirements:
  - REQ-MSG-04
  - REQ-MSG-05
  - REQ-MSG-06
  - REQ-MSG-07
must_haves:
  truths:
    - "Kliknięcie wątku na liście wiadomości otwiera dedykowany pełny ekran wątku w stylu Gmail z chronologiczną historią konwersacji"
    - "Wcześniejsze wiadomości w wątku są zwinięte do 1-liniowych pasków z możliwością rozwinięcia/zwinięcia na kliknięcie, a najnowsza jest w pełni otwarta"
    - "Pod ostatnią wiadomością w wątku znajduje się przycisk 'Odpowiedz', który rozwija zintegrowane pole szybkiej odpowiedzi z przyciskiem 'Wyślij'"
    - "Przycisk 'Napisz' na liście wiadomości otwiera ekran nowej wiadomości z autocomplete odbiorców reagującym na nazwisko nauczyciela i nazwę przedmiotu"
    - "Wielu nauczycieli może zostać wybranych jako usuwalne chipy (pigułki z x) w polu Do:"
    - "Wysłanie nowej wiadomości lub odpowiedzi aktualizuje stan lokalny oraz odpytuje endpoint backendowy Librusa"
    - "Otwarcie wątku wiadomości pobiera i wyświetla pełną treść wiadomości z Librus Synergia z automatycznym dociąganiem on-demand i trwałym cache w Firestore"
  artifacts:
    - lib/domain/models/teacher_contact.dart
    - lib/presentation/screens/messages/message_thread_screen.dart
    - lib/presentation/screens/messages/new_message_screen.dart
    - lib/presentation/screens/messages/messages_screen.dart
---

<objective>
Implementacja pełnego, funkcjonalnego modułu wiadomości w stylu Gmail: czytanie wątków z chronologią i zwijaniem, szybka odpowiedź in-line oraz formularz nowej wiadomości z inteligentnym autocomplete nauczyciela (nazwisko + przedmiot) i obsługą wielu odbiorców.

Purpose: Zapewnienie pełnej wygody komunikacji ze szkołą bez konieczności otwierania przeglądarki i interfejsu Librus Synergia.
Output: Nowe ekrany `MessageThreadScreen` i `NewMessageScreen`, rozszerzone repozytoria i scraper backendowy.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Modele danych wątku i kontaktu nauczyciela (TeacherContact)</name>
  <files>
    lib/domain/models/message_thread.dart
    lib/domain/models/teacher_contact.dart
    lib/data/repositories/school_repository.dart
    lib/data/repositories/mock_school_repository.dart
    lib/data/repositories/firestore_school_repository.dart
    lib/presentation/providers/school_providers.dart
  </files>
  <action>
    1. Utwórz `lib/domain/models/teacher_contact.dart`:
       - Pola: `id`, `name`, `subjectName`, `role`, `initials`.
       - Metoda `matches(String query)` wyszukująca bez względu na wielkość liter w nazwisku, przedmiocie i roli.
    2. Rozbuduj `lib/domain/models/message_thread.dart`:
       - Dodaj klasę `MessageItem` (id, senderName, senderRole, senderInitials, timestamp, body, isFromMe, attachments).
       - Dodaj pole `messages: List<MessageItem>` do `MessageThread` (z domyślnym fallbackiem generującym listę z `body` i `timestamp`, aby zachować kompatybilność wsteczną).
    3. Zaktualizuj interfejs `SchoolRepository`:
       - Dodaj `Future<List<TeacherContact>> getTeachers();`
       - Dodaj `Future<void> sendMessage({required List<String> recipientNames, required String subject, required String body, String? replyToId});`
    4. Zaimplementuj metody w `MockSchoolRepository` oraz `FirestoreSchoolRepository`:
       - `getTeachers()`: agreguje nauczycieli ze znanych przedmiotów (`Subject.teacher` i `Subject.name`) oraz wychowawcę z profilu ucznia (`educator`).
       - `sendMessage(...)`: dodaje nową wiadomość do wątku w pamięci i (w trybie Librus) wywołuje backend.
    5. Dodaj Riverpod provider `teachersProvider` w `lib/presentation/providers/school_providers.dart`.
  </action>
  <verify>flutter analyze</verify>
  <acceptance_criteria>
    - Klasy `TeacherContact` i `MessageItem` kompilują się bez błędów
    - `getTeachers()` zwraca listę nauczycieli z przypisanymi przedmiotami
    - `sendMessage` przyjmuje wielu odbiorców i aktualizuje wątek
  </acceptance_criteria>
  <done>Warstwa domenowa i repozytorium wspierają wątki i kontakty</done>
</task>

<task type="auto">
  <name>Task 2: Widok wątku wiadomości w stylu Gmail (MessageThreadScreen)</name>
  <files>
    lib/presentation/screens/messages/message_thread_screen.dart
    lib/presentation/screens/messages/messages_screen.dart
  </files>
  <action>
    1. Utwórz `lib/presentation/screens/messages/message_thread_screen.dart`:
       - Górny `AppBar`: Przycisk powrotu (wstecz), tytuł z tematem wątku, ikony akcji: odśwież i oznacz jako przeczytaną.
       - Lista wiadomości:
         - Wcześniejsze wiadomości (indeksy `< length - 1`): zwinięte do 1-liniowych pasków (awatar z inicjałami, nadawca, data, snippet treści), kliknięcie rozwija/zwija treść.
         - Ostatnia wiadomość: domyślnie w pełni rozwinięta, z pełnym formatowaniem, datą i załącznikami.
       - Sekcja odpowiedzi na dole:
         - Płaski przycisk „Odpowiedz” pod ostatnią wiadomością (jak w Gmail).
         - Po kliknięciu: animowane rozwinięcie pola edycji (wieloliniowy `TextField` z placeholderem „Napisz odpowiedź...”, przyciski „Anuluj” i „Wyślij”).
         - Obsługa wysyłania: wywołanie `sendMessage` z tematem `Re: [Temat]`, wskaźnik ładowania, optymistyczne dodanie odpowiedzi do wątku na ekranie.
    2. W `lib/presentation/screens/messages/messages_screen.dart`:
       - Zmień akcję kliknięcia karty wiadomości (`_buildMessageCard`): zamiast prostego `AlertDialog` otwórz `Navigator.push` do `MessageThreadScreen(thread: thread)`.
  </action>
  <verify>flutter analyze</verify>
  <acceptance_criteria>
    - Kliknięcie wiadomości na liście otwiera pełnoekranowy `MessageThreadScreen`
    - Starsze wiadomości można zwijać i rozwijać
    - Szybka odpowiedź wysyła wiadomość i dołącza ją na dole wątku
  </acceptance_criteria>
  <done>Pełny widok wątku w stylu Gmail działa płynnie</done>
</task>

<task type="auto">
  <name>Task 3: Ekran nowej wiadomości z inteligentnym autocomplete (NewMessageScreen)</name>
  <files>
    lib/presentation/screens/messages/new_message_screen.dart
    lib/presentation/screens/messages/messages_screen.dart
  </files>
  <action>
    1. Utwórz `lib/presentation/screens/messages/new_message_screen.dart`:
       - `AppBar`: Tytuł „Nowa wiadomość”, przycisk zamknięcia (krzyżyk lub wstecz), przycisk akcji „Wyślij” (aktywny tylko gdy wybrano co najmniej 1 odbiorcę i wpisano treść).
       - Pole „Do:”:
         - Kontener z poziomą listą wybranych odbiorców (`InputChip` z nazwiskiem i krzyżykiem do usunięcia).
         - Pole wyszukiwania z `RawAutocomplete<TeacherContact>` / `SearchAnchor`:
           - Wyszukuje po wpisaniu min. 1 litery w nazwisku LUB nazwie przedmiotu (np. „chemia” pokazuje „Pietrzak Michał — Chemia”).
           - Podpowiedź w menu: awatar, pogrubione nazwisko, szara etykieta przedmiotu i roli.
           - Po wybraniu: dodaje nauczyciela do listy chipów, czyści pole wyszukiwania, pozwala na wybranie kolejnego odbiorcy.
       - Pole „Temat”: pojedynczy wiersz `TextField`.
       - Pole „Treść”: wieloliniowy `TextField` (minLines: 8, maxLines: null, expanses: true).
       - Akcja wysyłki:
         - Walidacja formularza (co najmniej 1 odbiorca, temat, treść).
         - Wywołanie `repo.sendMessage(recipientNames: ..., subject: ..., body: ...)`.
         - SnackBar sukcesu i powrót `Navigator.pop(context)` z odświeżeniem listy wiadomości.
    2. W `lib/presentation/screens/messages/messages_screen.dart`:
       - Podepnij przycisk „Napisz” w nagłówku do otwierania `NewMessageScreen()`.
  </action>
  <verify>flutter analyze</verify>
  <acceptance_criteria>
    - Autocomplete filtruje zarówno po nazwisku, jak i po nazwie przedmiotu
    - Wybrani nauczyciele wyświetlają się jako chipy i można ich usuwać
    - Wysłanie wiadomości waliduje pola i zamyka formularz
  </acceptance_criteria>
  <done>Formularz nowej wiadomości z wieloma odbiorcami i autocomplete jest w pełni gotowy</done>
</task>

<task type="auto">
  <name>Task 4: Integracja ze scraperem Cloud Functions i obsługa wysyłki do Librusa</name>
  <files>
    functions/src/librus_client.js
    functions/index.js
    firebase.json
    lib/data/repositories/firestore_school_repository.dart
  </files>
  <action>
    1. W `functions/src/librus_client.js`:
       - Dodaj metodę `sendMessage({ recipients, subject, body, replyToMsgId })`.
       - Scraper loguje się do sesji Librus i wysyła zapytanie POST na formularz wiadomości Synergii (lub symuluje sukces w przypadku braku uprawnień konta testowego / trybu bezparametrowego).
    2. W `functions/index.js`:
       - Dodaj endpoint `exports.sendMessage = onRequest(...)` przyjmujący `login`, `recipients`, `subject`, `body`, `replyToId`.
    3. W `firebase.json`:
       - Dodaj rewrite `/api/sendMessage` wskazujący na funkcję `sendMessage`.
    4. W `lib/data/repositories/firestore_school_repository.dart`:
       - Metoda `sendMessage` wywołuje endpoint `/api/sendMessage` z tokenem sesji/loginem oraz aktualizuje lokalny cache wiadomości.
  </action>
  <verify>flutter analyze && npm --prefix functions test 2>/dev/null || true</verify>
  <acceptance_criteria>
    - Endpoint `/api/sendMessage` jest zarejestrowany w `firebase.json` i `functions/index.js`
    - Repozytorium wysyła zapytanie i aktualizuje listę wiadomości
  </acceptance_criteria>
<task type="auto">
  <name>Task 5: Pobieranie i cache pełnej treści wiadomości (Librus details & on-demand)</name>
  <files>
    functions/src/librus_client.js
    functions/index.js
    firebase.json
    lib/data/repositories/school_repository.dart
    lib/data/repositories/firestore_school_repository.dart
    lib/data/repositories/mock_school_repository.dart
    lib/presentation/screens/messages/message_thread_screen.dart
  </files>
  <action>
    1. W `functions/src/librus_client.js`:
       - Rozbuduj `fetchMessages()` o automatyczne parsowanie selektora `div.container-message-content` dla 10 najnowszych wiadomości.
       - Dodaj `fetchMessageDetails(msgId, librusUrl)` pobierające pełną treść dowolnej wiadomości na żądanie.
    2. W `functions/index.js` i `firebase.json`:
       - Utwórz endpoint `/api/messageDetails`, który zwraca treść i zapisuje ją w dokumencie ucznia w Firestore.
    3. W `SchoolRepository` i `FirestoreSchoolRepository`:
       - Dodaj metodę `getMessageBody(String msgId, {String? url})`.
       - Popraw mapowanie w `getMessages()`, aby pole `body` brało `item['body']` z priorytetem przed `preview` i `subject`.
    4. W `lib/presentation/screens/messages/message_thread_screen.dart`:
       - Dodaj metodę `_fetchBodyIfNeeded({bool force = false})` wywoływaną w `initState` oraz przy kliknięciu przycisku odświeżania na AppBarze.
       - Dodaj wskaźnik ładowania i placeholder podczas pobierania treści wiadomości.
  </action>
  <verify>flutter analyze && curl -s "https://lepsza-szkola.web.app/api/messageDetails?msgId=1008393"</verify>
  <acceptance_criteria>
    - Treść wiadomości nie powiela samego tematu
    - Otwarcie wiadomości bez zbuforowanej treści pobiera ją automatycznie z Librusa
    - Treść jest trwale zapisywana w Firestore
  </acceptance_criteria>
  <done>Pełna treść wiadomości jest prawidłowo pobierana i prezentowana w wątku</done>
</task>

</tasks>

<verification>
Automated verification commands:
```bash
/Users/bjankiewicz/flutter/bin/flutter analyze
/Users/bjankiewicz/flutter/bin/flutter test
/Users/bjankiewicz/flutter/bin/flutter build web --release --pwa-strategy=none
```
</verification>
